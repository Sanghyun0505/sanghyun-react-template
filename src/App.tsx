import Provider from "@/components/provider/Provider";
import Router from "@/router/Router";

const App = () => {
  return (
    <Provider>
      <Router />
    </Provider>
  );
};

export default App;
